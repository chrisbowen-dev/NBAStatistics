import { describe, it } from 'vitest';

describe('PlayerAvatar', () => {
	it.todo('renders the headshot image for a valid player id');
	it.todo('renders the fallback when no player id is provided');
	it.todo('renders the fallback after the image fails to load');
});
